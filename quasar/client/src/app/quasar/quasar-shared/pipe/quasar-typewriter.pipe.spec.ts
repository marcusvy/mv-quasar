import { QuasarTypewriterPipe } from './quasar-typewriter.pipe';

describe('QuasarTypewriterPipe', () => {
  it('create an instance', () => {
    const pipe = new QuasarTypewriterPipe();
    expect(pipe).toBeTruthy();
  });
});
