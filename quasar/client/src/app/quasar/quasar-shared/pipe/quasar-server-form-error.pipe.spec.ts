import { QuasarServerFormErrorPipe } from './quasar-server-form-error.pipe';

describe('QuasarServerFormErrorPipe', () => {
  it('create an instance', () => {
    const pipe = new QuasarServerFormErrorPipe();
    expect(pipe).toBeTruthy();
  });
});
