import { ControlOptionsDirective } from './control-options.directive';

describe('ControlOptionsDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlOptionsDirective();
    expect(directive).toBeTruthy();
  });
});
