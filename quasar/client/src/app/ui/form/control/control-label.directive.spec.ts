import { ControlLabelDirective } from './control-label.directive';

describe('ControlLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlLabelDirective();
    expect(directive).toBeTruthy();
  });
});
