import { ControlValueDirective } from './control-value.directive';

describe('ControlValueDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlValueDirective();
    expect(directive).toBeTruthy();
  });
});
