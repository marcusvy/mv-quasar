import { ControlSetupDirective } from './control-setup.directive';

describe('ControlSetupDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlSetupDirective();
    expect(directive).toBeTruthy();
  });
});
