import { ControlMessageDirective } from './control-message.directive';

describe('ControlMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlMessageDirective();
    expect(directive).toBeTruthy();
  });
});
