import { ControlContainerDirective } from './control-container.directive';

describe('ControlContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlContainerDirective();
    expect(directive).toBeTruthy();
  });
});
