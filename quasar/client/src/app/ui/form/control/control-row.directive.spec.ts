import { ControlRowDirective } from './control-row.directive';

describe('ControlRowDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlRowDirective();
    expect(directive).toBeTruthy();
  });
});
