import { ButtonTopDirective } from './button-top.directive';

describe('ButtonTopDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonTopDirective();
    expect(directive).toBeTruthy();
  });
});
