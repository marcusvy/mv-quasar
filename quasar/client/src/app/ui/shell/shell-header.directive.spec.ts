import { ShellHeaderDirective } from './shell-header.directive';

describe('ShellHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ShellHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
