import { ShellRightDirective } from './shell-right.directive';

describe('ShellRightDirective', () => {
  it('should create an instance', () => {
    const directive = new ShellRightDirective();
    expect(directive).toBeTruthy();
  });
});
