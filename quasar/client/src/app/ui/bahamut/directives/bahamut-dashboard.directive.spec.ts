import { BahamutDashboardDirective } from './bahamut-dashboard.directive';

describe('BahamutDashboardDirective', () => {
  it('should create an instance', () => {
    const directive = new BahamutDashboardDirective();
    expect(directive).toBeTruthy();
  });
});
