/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ListItemSecondaryDirective } from './list-item-secondary.directive';

describe('Directive: ListItemSecondary', () => {
  it('should create an instance', () => {
    let directive = new ListItemSecondaryDirective();
    expect(directive).toBeTruthy();
  });
});
