/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ListItemPrimaryDirective } from './list-item-primary.directive';

describe('Directive: ListItemPrimary', () => {
  it('should create an instance', () => {
    let directive = new ListItemPrimaryDirective();
    expect(directive).toBeTruthy();
  });
});
